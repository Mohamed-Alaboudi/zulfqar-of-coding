# Voice pack manifest

Audited registered skill name: `voice-agent-qa`.

This is an inventory, not redistributed source. Phone numbers, recordings, transcripts, webhook payloads, and provider credentials must never be committed.
